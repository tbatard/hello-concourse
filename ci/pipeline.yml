resources:
- name: master
  type: git
  source:
    uri: {{git-repo}}
    branch: master
    private_key: {{git-private-key}}

jobs:
- name: build
  serial: true
  plan:
  - get: master
    trigger: true
  - task: build
    file: master/ci/tasks/build.yml
